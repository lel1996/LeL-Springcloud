<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lierlin.dao.UserMapper">
    <select id="queryById" resultType="com.lierlin.pojo.User" parameterType="integer">
    select * from user where id = #{id}
  </select>
    <select id="queryList" resultType="com.lierlin.pojo.User">
    select * from user
  </select>
    <select id="add" parameterType="com.lierlin.pojo.User" resultType="java.lang.Boolean">
    insert into user values (''，'','')
  </select>
</mapper>